node_modules/
dist/
build/
.env
.env.local
*.log
.DS_Store

# agent sandbox: generated by runs, never committed
/sandbox/
/runs/*.json
!/runs/.gitkeep
# One recorded run is checked in so a fresh clone has something to replay
# through the scrubber immediately. The filename has to stay equal to the runId
# inside it — that is how the loader finds it.
!/runs/rehearsal-mt2r8dp0-c730.json
